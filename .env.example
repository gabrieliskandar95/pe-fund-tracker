# Copy this to .env and fill in values before running locally.
# Railway sets PORT automatically — do not set it manually in Railway.

# Optional: override the SQLite database file path
# DB_PATH=pe_funds.db

# Optional: set log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO
